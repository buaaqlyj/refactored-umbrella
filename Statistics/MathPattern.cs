using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Statistics
{
    public enum MatchPattern { FullyMatch = 2, GeneralMatch = 1, DoNotNecessary = -1, DoNotMatch = 0 };

    public enum Flag { Neg = -1, Zero = 0, Pos = 1 };
}


